// Layout components
export { default as SiteNav } from './site-nav'
export { default as NavWrapper } from './nav-wrapper' // SSR-safe navigation wrapper
export { default as Header } from './header' // Keep the old header for backward compatibility
